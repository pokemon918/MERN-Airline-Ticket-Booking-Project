import { createTheme } from "@material-ui/core";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#F02A4B",
      contrastText: "#fff",
    },
    secondary: {
      main: "#000000",
    },
  },
});

export default Theme;
